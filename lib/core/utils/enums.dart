enum ToastStates { success, error, warning }

enum RequestState {
  initial,
  loading,
  success,
  error,
  paginationLoading,
  paginationError,
  noMoreData
}

enum DefaultRequestState { initial, loading, success, error }
